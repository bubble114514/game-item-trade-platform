server:
  port: 8086

spring:
  application:
    name: message-service
  datasource:
    url: jdbc:mysql://192.168.201.100:3306/game_message?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.201.100:8848
        namespace:

rocketmq:
  name-server: 192.168.201.100:9876

management:
  endpoints:
    web:
      exposure:
        include: health,info
