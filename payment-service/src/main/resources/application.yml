server:
  port: 8085

spring:
  application:
    name: payment-service
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.201.100:8848
        namespace: 

management:
  endpoints:
    web:
      exposure:
        include: health,info
