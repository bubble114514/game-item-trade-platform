server:
  port: 8085

spring:
  application:
    name: payment-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
        namespace: ${NACOS_NAMESPACE:}

management:
  endpoints:
    web:
      exposure:
        include: health,info
